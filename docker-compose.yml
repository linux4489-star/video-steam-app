version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=production-secret-key-2026
      - NODE_ENV=production
    volumes:
      - ./uploads:/tmp/uploads
    restart: unless-stopped
